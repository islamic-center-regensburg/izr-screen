stages:
  - deploy

deploy_izr_screen:
  stage: deploy
  variables:
    TARGET: screen
    BRANCH: normal-mode
    SUBMODULE: apps/screen
  trigger:
    include: 
      - project: izr-production/izr-server
        file: .gitlab-ci.yml
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'